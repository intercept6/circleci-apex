version: 2

jobs:
  deploy:
    docker:
      - image: kakakakakku/apex
    steps:
      - checkout
      - run: apex version
      - run: apex deploy hello

workflows:
  version: 2
  apex:
    jobs:
      - deploy
